<form method="post" action="{{ path('app_fizzbuzz_delete', {'id': fizzbuzz.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fizzbuzz.id) }}">
    <button class="btn">Delete</button>
</form>
